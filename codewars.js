function printerError(s) {

	const goodLetters = "abcdefghijklm"

	let errors = 0

	let totalLetters = s.length

	for (const letters in s) {
		if (!(letters in goodLetters)) {
			const element = object[key];
			
		}
	}

	
}